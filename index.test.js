/* eslint-disable no-undef */
const expect = require('chai').expect;
const calc = require('./index');

describe('bbb5', function () {
  it('todo', function () {
    expect(calc.calcDistance()).to.equal('TODO');
  });
});
